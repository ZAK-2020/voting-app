{
  "name": "voting-app",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install-backend": "npm install --prefix backend",
    "install-frontend": "npm install --prefix frontend",
    "build": "npm run install-backend && npm run install-frontend && CI=false npm run build --prefix frontend",
    "start": "node backend/server.js"
  }
}
